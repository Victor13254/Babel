# --- Base ---
PROJECT_NAME=Babel
ENV=dev
API_V1_PREFIX=/api
BACKEND_CORS_ORIGINS=["http://localhost:5173"]

# --- Seguridad ---
SECRET_KEY=changeme_in_prod                # Cambiar en producción
ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_ALGORITHM=HS256
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=100

# --- DB (Postgres en Docker) ---
POSTGRES_USER=babel
POSTGRES_PASSWORD=babelpwd                 # Cambiar en producción
POSTGRES_DB=babel
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg2://babel:babelpwd@db:5432/babel

# --- Admin inicial ---
ADMIN_EMAIL=admin@babel.local
ADMIN_PASSWORD=admin                       # Cambiar en producción

# --- Redis ---
REDIS_URL=redis://redis:6379/0

# --- S3 / MinIO (para backups locales) ---
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin                   # Cambiar en producción
S3_SECRET_KEY=minioadmin                   # Cambiar en producción
S3_BUCKET=babel-backups
S3_REGION=us-east-1
S3_USE_SSL=false
S3_PUBLIC_BASE_URL=http://localhost:9000/babel-backups
S3_PRESIGN_EXPIRES= 900
# --- Frontend ---
VITE_API_BASE=http://localhost:8000
